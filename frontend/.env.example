# Local development — Vite proxy handles /api → localhost:8000
# Leave VITE_API_URL blank for local dev (uses proxy in vite.config.js)
VITE_API_URL=

# Production (Vercel) — set this to your Render backend URL
# VITE_API_URL=https://your-app.onrender.com
